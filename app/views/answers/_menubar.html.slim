ul.nav.nav-pills.menubar
  li = content_tag 'a', class: 'edit-answer-link', data: {answer_id: answer.id} do
         span.glyphicon.glyphicon-pencil
  li = link_to question_answer_path(answer.question, answer), method: :delete, remote:true, data: {confirm: 'Are you sure?'}, class: 'delete-answer-link' do
         span.glyphicon.glyphicon-scissors

